{
  "name": "ssrs",
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "spago build",
    "test": "spago -x tests.dhall test",
    "repl": "spago -x tests.dhall repl",
    "bench": "spago -x bench.dhall run -m Benchmark.Main"
  },
  "devDependencies": {
    "benchmark": "^2.1.4"
  }
}
