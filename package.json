{
  "name": "ssrs",
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "spago build",
    "bench": "spago -x bench.dhall run -m Benchmark.Main"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "purescript": "^0.14.9",
    "spago": "^0.20.7"
  }
}
